name: skillcheck
description: Scan AI agent skills, rule files, and MCP configs for prompt injections and dangerous permissions.
branding:
  icon: shield
  color: blue

inputs:
  path:
    description: Path to scan.
    required: false
    default: "."
  fail-on:
    description: "Severity threshold that fails the job: high | medium | low | info."
    required: false
    default: high
  format:
    description: "Output format: pretty | json | sarif | md."
    required: false
    default: pretty
  output:
    description: Optional file to write the report to (e.g. skillcheck.sarif).
    required: false
    default: ""
  version:
    description: skillcheck version specifier to install (e.g. "==0.1.0").
    required: false
    default: ""

runs:
  using: composite
  steps:
    - name: Install skillcheck
      shell: bash
      run: python -m pip install --disable-pip-version-check "skillcheck${{ inputs.version }}"
    - name: Run skillcheck
      shell: bash
      run: |
        args=(scan "${{ inputs.path }}" --fail-on "${{ inputs.fail-on }}" --format "${{ inputs.format }}")
        if [ -n "${{ inputs.output }}" ]; then
          args+=(--output "${{ inputs.output }}")
        fi
        skillcheck "${args[@]}"
